# Frontend Environment Variables
# Copy this file to .env and adjust values as needed

# Backend Configuration
# Port where the backend Flask server is running
VITE_BACKEND_PORT=5001

# Host where the backend Flask server is running
# Use 'localhost' for local development
# Use the Pi's IP address when connecting to remote Pi
VITE_BACKEND_HOST=localhost

# Alternative: You can also use FLASK_PORT which will be used as fallback
# FLASK_PORT=5001