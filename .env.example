# Piano LED Visualizer - Environment Configuration
# Copy this file to .env and adjust values as needed

# Backend Flask Server Configuration
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=5001

# GPIO and Hardware Configuration
# Note: BLINKA environment variables are not needed for rpi_ws281x LED library
# and can cause conflicts. They are only required for CircuitPython/Adafruit libraries.

# Network Configuration
# PI_IP=192.168.1.225
# PI_USER=pi